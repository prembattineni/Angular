import { Searchpipe1Pipe } from './searchpipe1.pipe';

describe('Searchpipe1Pipe', () => {
  it('create an instance', () => {
    const pipe = new Searchpipe1Pipe();
    expect(pipe).toBeTruthy();
  });
});
